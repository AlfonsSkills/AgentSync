# v0.6.0

Release Date: 2026-01-14

## 🎯 Highlights

- **智能 Remove 命令**: `remove` 命令现在会检查 skill 是否存在，仅显示有效的工具选项
- **工具选项排序优化**: 终端工具优先显示，IDE 工具在后

## ✨ New Features

### Remove 命令存在性检查

- 选择目标工具前，自动检查 skill 在各个工具的全局目录中是否存在
- 选项列表中只展示存在该 skill 的工具，避免用户选择无效目标
- 选择工具后，检查项目目录中是否存在该 skill，仅当存在才提示 "Also remove from project directory?"
- 如果 skill 在所有工具中都不存在，直接报错提示

### 工具顺序优化

终端工具和 IDE 工具分类排序，终端工具优先：

1. **终端工具**: Gemini CLI, Claude Code, Codex CLI, OpenCode
2. **IDE 工具**: Antigravity IDE, GitHub Copilot, Cursor

## 📁 File Changes

### Modified Files

- `cmd/interactive.go`
  - 新增 `checkSkillExistsInProviders()` - 检查 skill 在哪些工具中存在
  - 新增 `checkSkillExistsInProject()` - 检查 skill 是否在项目目录存在
  - 新增 `resolveTargetProvidersForRemove()` - 为 remove 过滤工具列表
  - 新增 `resolveRemoveScopeWithCheck()` - 带存在性检查的删除范围解析

- `cmd/remove.go` - 使用新的存在性检查函数

- `internal/target/registry.go`
  - 更新 `AllToolTypes()` 返回顺序
  - 更新错误信息中的工具顺序

- `cmd/root.go` - 更新 `--target` 帮助文档中的工具顺序

## 🔧 Breaking Changes

None. All existing CLI commands remain backward compatible.

## 📦 Usage

```bash
# Remove 命令现在只显示存在 skill 的工具
❯ skillsync remove my-skill
🗑️  Preparing to remove: my-skill

? Select target tools:  # 仅显示存在 my-skill 的工具
> [ ]  Antigravity IDE
```

## 📦 Installation

```bash
curl -fsSL https://raw.githubusercontent.com/AlfonsSkills/SkillSync/main/install.sh | bash
```
