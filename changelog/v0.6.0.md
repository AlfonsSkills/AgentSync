# v0.6.0

Release Date: 2026-01-14

## 🎯 Highlights

- **Smart Remove Command**: The `remove` command now checks if skills exist, showing only valid tool options
- **Tool Order Optimization**: Terminal tools are prioritized over IDE tools in selection lists

## ✨ New Features

### Remove Command Existence Check

- Before selecting target tools, automatically checks if the skill exists in each tool's global directory
- Only tools containing the skill are displayed in the options list, preventing invalid selections
- After tool selection, checks if the skill exists in the project directory before prompting "Also remove from project directory?"
- If the skill doesn't exist in any tool, displays a clear error message

### Tool Order Optimization

Tools are now sorted by category, with terminal tools first:

1. **Terminal Tools**: Gemini CLI, Claude Code, Codex CLI, OpenCode
2. **IDE Tools**: Antigravity IDE, GitHub Copilot, Cursor

## 📁 File Changes

### Modified Files

- `cmd/interactive.go`
  - Added `checkSkillExistsInProviders()` - checks which tools contain the skill
  - Added `checkSkillExistsInProject()` - checks if skill exists in project directory
  - Added `resolveTargetProvidersForRemove()` - filters tool list for remove operation
  - Added `resolveRemoveScopeWithCheck()` - scope resolution with existence checking

- `cmd/remove.go` - Uses new existence check functions

- `internal/target/registry.go`
  - Updated `AllToolTypes()` return order
  - Updated tool order in error messages

- `cmd/root.go` - Updated tool order in `--target` help documentation

## 🔧 Breaking Changes

None. All existing CLI commands remain backward compatible.

## 📦 Usage

```bash
# Remove command now only shows tools containing the skill
❯ skillsync remove my-skill
🗑️  Preparing to remove: my-skill

? Select target tools:  # Only shows tools where my-skill exists
> [ ]  Antigravity IDE
```

## 📦 Installation

```bash
curl -fsSL https://raw.githubusercontent.com/AlfonsSkills/SkillSync/main/install.sh | bash
```
