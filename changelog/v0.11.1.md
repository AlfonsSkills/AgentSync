# v0.11.1 (2026-01-20)

## 🐛 Bug Fixes

### Static Compilation Fix
- **Fix Linux compatibility issue**: Add `CGO_ENABLED=0` build flag to produce fully static binaries
- Resolves `GLIBC_2.32/GLIBC_2.34 not found` errors on older Linux systems (e.g., CentOS 7)
- Both `build` and `cross` targets now generate executables with no external dependencies

## 📁 Changed Files
- `Makefile` - Add `CGO_ENABLED=0` to build targets
